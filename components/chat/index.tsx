import { Card, CardHeader, CardTitle } from "../ui/card";

export default function Chat() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Chat</CardTitle>
      </CardHeader>
    </Card>
  );
}
